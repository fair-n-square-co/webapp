# Generated by the TanStack Start plugin during `vite dev` / `vite build`.
# Committed so `tsc --noEmit` and editors work on a fresh clone without a build
# first; CI reruns the generator and fails if this file is stale.
src/routeTree.gen.ts linguist-generated=true
